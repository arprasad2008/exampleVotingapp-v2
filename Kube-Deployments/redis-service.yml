apiVersion: v1
type: Service
metadata:
  name: redis
  labels:
    name: redis-service
    app: demo-voting-app
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
      app: demo-voting-app
      name: redis-pod


